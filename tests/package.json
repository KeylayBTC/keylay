{
  "name": "keylay-tests",
  "private": true,
  "description": "Playwright end-to-end tests for Keylay. Not part of the production app.",
  "scripts": {
    "test": "npx playwright test --config ../playwright.config.js",
    "test:ui": "npx playwright test --ui --config ../playwright.config.js",
    "test:headed": "npx playwright test --headed --config ../playwright.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
